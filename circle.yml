dependencies:
  post:
    - sudo apt-get update
    - sudo apt-get install -y qemu-system-arm
    - sudo service docker start
    - docker pull markfirmware/ufpc
test:
  override:
    - ./docker-test.sh
